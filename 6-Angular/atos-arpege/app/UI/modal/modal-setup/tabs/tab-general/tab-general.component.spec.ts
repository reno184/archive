/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MissionGeneraleComponent } from './tab-general.component';

import { FormBuilder, FormsModule, ReactiveFormsModule } from '@angular/forms';
import { AppInitService } from '../../../../../app.init';
import { HttpClient, HttpHandler } from '@angular/common/http';
import { SetupFormService } from '../../../../../shared/services/setup-form.service';

describe('MissionGeneraleComponent', () => {
    let component: MissionGeneraleComponent;
    let fixture: ComponentFixture<MissionGeneraleComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [MissionGeneraleComponent],
            imports: [ReactiveFormsModule, FormsModule],
            providers:[
{provide: AppInitService, useValue: { staticDatas: { labels: {} } }}
]
        }).compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(MissionGeneraleComponent);
        component = fixture.componentInstance;
        //  spyOn(component, 'getMode').and.returnValue([{ val: '0', lib: 'r' }]);
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
*/
