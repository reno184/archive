/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FoCoveragePanelComponent } from './fo-coverage-panel.component';
import { ReactiveFormsModule } from '@angular/forms';

describe('FoCoveragePanelComponent', () => {
  let component: FoCoveragePanelComponent;
  let fixture: ComponentFixture<FoCoveragePanelComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [ReactiveFormsModule],
      declarations: [FoCoveragePanelComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FoCoveragePanelComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
