/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ArcAttachedPanelComponent } from './arc-attached-panel.component';
import { ReactiveFormsModule } from '@angular/forms';
import { FormDirectivesModule } from '../../../../../../../shared/directives/form-directives.module';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';

describe('ArcAttachedPanelComponent', () => {
  let component: ArcAttachedPanelComponent;
  let fixture: ComponentFixture<ArcAttachedPanelComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [ReactiveFormsModule, FormDirectivesModule],
      declarations: [ArcAttachedPanelComponent],
      providers:[
{provide: AppInitService, useValue: { staticDatas: { labels: {} } }}
],
      schemas: [CUSTOM_ELEMENTS_SCHEMA]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ArcAttachedPanelComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
