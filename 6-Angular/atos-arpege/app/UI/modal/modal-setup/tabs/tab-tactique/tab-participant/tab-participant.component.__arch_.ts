/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TabParticipantComponent } from './tab-participant.component';
import { ReactiveFormsModule } from '@angular/forms';

describe('TabParticipantComponent', () => {
  let component: TabParticipantComponent;
  let fixture: ComponentFixture<TabParticipantComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [TabParticipantComponent],
      imports: [ReactiveFormsModule]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TabParticipantComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
