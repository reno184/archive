/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MissionSitutactiqueComponent } from './tab-tactique.component';
import { ReactiveFormsModule } from '@angular/forms';
import { RouterTestingModule } from '@angular/router/testing';

describe('Mis/!**!/sionSitutactiqueComponent', () => {
    let component: MissionSitutactiqueComponent;
    let fixture: ComponentFixture<MissionSitutactiqueComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [MissionSitutactiqueComponent],
            imports: [ReactiveFormsModule, RouterTestingModule.withRoutes([])],
        }).compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(MissionSitutactiqueComponent);
        component = fixture.componentInstance;
        /!*    spyOn(component, 'getClasses').and.returnValue([
            { val: 'r', lib: 'r' },
        ]);*!/
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
*/
