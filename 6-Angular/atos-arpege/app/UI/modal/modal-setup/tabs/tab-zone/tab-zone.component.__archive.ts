/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MissionPoszoneComponent } from './tab-zone.component';
import { ReactiveFormsModule } from '@angular/forms';
import { AppInitService } from '../../../../../app.init';
import { SetupFormService } from '../../../../../shared/services/setup-form.service';
import { HttpClient, HttpHandler } from '@angular/common/http';
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';

describe('MissionPoszoneComponent', () => {
    let component: MissionPoszoneComponent;
    let fixture: ComponentFixture<MissionPoszoneComponent>;

    beforeEach(async(() => {
        TestBed.configureTestingModule({
            declarations: [MissionPoszoneComponent],
            imports: [ReactiveFormsModule],
            schemas: [CUSTOM_ELEMENTS_SCHEMA],
            providers: [
                SetupFormService,
                {provide: AppInitService, useValue: { configDatas: { providers: basic : {} }, confFromServer : { MAP_URL : '' }}
            ],
        }).compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(MissionPoszoneComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
*/
